<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>All Users</h1>
    {{#if users}}
    {{#each users}}
    {{#if @first}}

    {{else}}
    <ul>
        <li><a href="/chat/{{id}}">{{id}}</a></li>
    </ul>
    {{/if}}
    {{/each}}
    {{/if}}
</body>
</html>